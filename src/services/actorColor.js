/**
 * Colour per actor, assigned by cast rank.
 *
 * Roles are ranked by how many lines they speak, and rank picks the palette
 * entry: the busiest role takes the first colour, the next takes the second,
 * and so on. The palette is ordered so that every prefix is the best palette
 * of that size, which means the characters on stage most often are the ones
 * given the most clearly different colours, and near-duplicates only ever
 * reach walk-on parts.
 *
 * An earlier version derived the colour from a hash of the name. That kept
 * colours stable when a role was added, but it scattered a small cast across
 * the whole palette, so a twelve-role play could draw two neighbouring
 * desaturated teals and the two leads became impossible to tell apart. Rank
 * trades that stability away, deliberately: colours may shuffle when a script
 * is edited, and a role pinned through the picker keeps its colour regardless
 * because overrides are stored against the name.
 *
 * The palette is generated by scripts/gen-actor-palette.mjs. Every colour
 * clears WCAG AA on every background the app uses in both themes, and the set
 * stays separable under red-green colour vision deficiency. See that script
 * for the reasoning.
 */

// Generated by scripts/gen-actor-palette.mjs - do not edit by hand.
// Each entry is [light mode, dark mode].
const PALETTE = [
  ['oklch(0.26 0.07 126)', 'oklch(0.673 0.17 126)'],
  ['oklch(0.44 0.22 312)', 'oklch(0.833 0.115 312)'],
  ['oklch(0.335 0.135 24)', 'oklch(0.74 0.155 24)'],
  ['oklch(0.29 0.09 252)', 'oklch(0.7 0.16 252)'],
  ['oklch(0.305 0.135 336)', 'oklch(0.713 0.235 336)'],
  ['oklch(0.515 0.11 68)', 'oklch(0.9 0.07 68)'],
  ['oklch(0.47 0.19 260)', 'oklch(0.86 0.065 260)'],
  ['oklch(0.485 0.155 144)', 'oklch(0.873 0.235 144)'],
  ['oklch(0.5 0.085 194)', 'oklch(0.887 0.15 194)'],
  ['oklch(0.47 0.19 0)', 'oklch(0.86 0.08 0)'],
  ['oklch(0.35 0.21 280)', 'oklch(0.753 0.125 280)'],
  ['oklch(0.305 0.155 312)', 'oklch(0.713 0.205 312)'],
  ['oklch(0.455 0.105 158)', 'oklch(0.847 0.195 158)'],
  ['oklch(0.425 0.19 332)', 'oklch(0.82 0.155 332)'],
  ['oklch(0.32 0.13 0)', 'oklch(0.727 0.185 0)'],
  ['oklch(0.455 0.095 96)', 'oklch(0.847 0.17 96)'],
  ['oklch(0.29 0.095 40)', 'oklch(0.7 0.19 40)'],
  ['oklch(0.53 0.235 326)', 'oklch(0.913 0.075 326)'],
  ['oklch(0.425 0.18 346)', 'oklch(0.82 0.125 346)'],
  ['oklch(0.395 0.075 224)', 'oklch(0.793 0.14 224)'],
  ['oklch(0.485 0.185 32)', 'oklch(0.873 0.065 32)'],
  ['oklch(0.305 0.065 166)', 'oklch(0.713 0.145 166)'],
  ['oklch(0.395 0.16 10)', 'oklch(0.793 0.12 10)'],
  ['oklch(0.365 0.075 74)', 'oklch(0.767 0.16 74)'],
  ['oklch(0.35 0.15 348)', 'oklch(0.753 0.18 348)'],
  ['oklch(0.41 0.165 24)', 'oklch(0.807 0.105 24)'],
  ['oklch(0.5 0.095 174)', 'oklch(0.887 0.165 174)'],
  ['oklch(0.305 0.125 12)', 'oklch(0.713 0.18 12)'],
  ['oklch(0.395 0.09 114)', 'oklch(0.793 0.175 114)'],
  ['oklch(0.47 0.205 340)', 'oklch(0.86 0.1 340)'],
  ['oklch(0.365 0.17 326)', 'oklch(0.767 0.225 326)'],
  ['oklch(0.395 0.16 358)', 'oklch(0.793 0.13 358)'],
  ['oklch(0.5 0.125 150)', 'oklch(0.887 0.21 150)'],
  ['oklch(0.365 0.16 336)', 'oklch(0.767 0.2 336)'],
  ['oklch(0.395 0.065 202)', 'oklch(0.793 0.135 202)'],
  ['oklch(0.455 0.18 16)', 'oklch(0.847 0.08 16)'],
  ['oklch(0.44 0.18 354)', 'oklch(0.833 0.105 354)'],
  ['oklch(0.515 0.22 344)', 'oklch(0.9 0.065 344)'],
  ['oklch(0.305 0.13 350)', 'oklch(0.713 0.215 350)'],
  ['oklch(0.38 0.085 160)', 'oklch(0.78 0.175 160)'],
  ['oklch(0.35 0.14 8)', 'oklch(0.753 0.15 8)'],
  ['oklch(0.425 0.17 4)', 'oklch(0.82 0.105 4)'],
  ['oklch(0.32 0.145 330)', 'oklch(0.727 0.235 330)'],
  ['oklch(0.41 0.18 338)', 'oklch(0.807 0.15 338)'],
  ['oklch(0.335 0.14 354)', 'oklch(0.74 0.18 354)'],
  ['oklch(0.26 0.065 154)', 'oklch(0.673 0.17 154)'],
  ['oklch(0.365 0.07 174)', 'oklch(0.767 0.145 174)'],
  ['oklch(0.455 0.075 196)', 'oklch(0.847 0.14 196)']
];

const PALETTE_SIZE = PALETTE.length;

// Generated by scripts/gen-actor-palette.mjs - do not edit by hand.
// Hue-ordered ramp for the colour override picker.
const HUE_CHOICES = [
  ['oklch(0.565 0.23 0)', 'oklch(0.685 0.215 0)'],
  ['oklch(0.56 0.225 15)', 'oklch(0.685 0.205 15)'],
  ['oklch(0.56 0.225 30)', 'oklch(0.68 0.205 30)'],
  ['oklch(0.54 0.155 45)', 'oklch(0.7 0.2 45)'],
  ['oklch(0.53 0.125 60)', 'oklch(0.745 0.175 60)'],
  ['oklch(0.52 0.11 75)', 'oklch(0.805 0.17 75)'],
  ['oklch(0.535 0.11 90)', 'oklch(0.855 0.175 90)'],
  ['oklch(0.515 0.11 105)', 'oklch(0.92 0.195 105)'],
  ['oklch(0.525 0.125 120)', 'oklch(0.93 0.22 120)'],
  ['oklch(0.51 0.15 135)', 'oklch(0.805 0.235 135)'],
  ['oklch(0.505 0.14 150)', 'oklch(0.855 0.235 150)'],
  ['oklch(0.52 0.11 165)', 'oklch(0.88 0.185 165)'],
  ['oklch(0.52 0.095 180)', 'oklch(0.885 0.16 180)'],
  ['oklch(0.525 0.09 195)', 'oklch(0.88 0.15 195)'],
  ['oklch(0.52 0.09 210)', 'oklch(0.84 0.145 210)'],
  ['oklch(0.525 0.1 225)', 'oklch(0.765 0.145 225)'],
  ['oklch(0.52 0.12 240)', 'oklch(0.7 0.16 240)'],
  ['oklch(0.53 0.175 255)', 'oklch(0.66 0.18 255)'],
  ['oklch(0.355 0.235 270)', 'oklch(0.665 0.175 270)'],
  ['oklch(0.4 0.235 285)', 'oklch(0.67 0.175 285)'],
  ['oklch(0.44 0.235 300)', 'oklch(0.68 0.2 300)'],
  ['oklch(0.475 0.235 315)', 'oklch(0.69 0.235 315)'],
  ['oklch(0.515 0.235 330)', 'oklch(0.69 0.235 330)'],
  ['oklch(0.55 0.235 345)', 'oklch(0.69 0.235 345)']
];

/**
 * The colours offered by the override picker, in hue order.
 *
 * Deliberately not the palette. The palette is ordered so that every prefix is
 * well spread, which leaves its later entries looking like near duplicates of
 * earlier ones once they are laid out as a grid. Sorted by hue, neighbours are
 * meant to resemble each other and the set reads as a spectrum.
 */
export const hueChoices = () => HUE_CHOICES.map(([light, dark], index) => ({ index, light, dark }));

const paletteColor = (index) => {
  const [light, dark] = PALETTE[((index % PALETTE_SIZE) + PALETTE_SIZE) % PALETTE_SIZE];
  return { light, dark };
};

const hueColor = (index) => {
  const [light, dark] = HUE_CHOICES[((index % HUE_CHOICES.length) + HUE_CHOICES.length) % HUE_CHOICES.length];
  return { light, dark };
};

/** True for the pseudo-actor that stands in for unattributed stage directions. */
export function isStageDirections(actor) {
  return !actor || actor === 'undefined';
}

/**
 * Speaking roles in rank order: most lines first.
 *
 * Ties are broken by name, and by code unit rather than localeCompare, which
 * varies by locale and would make the colours depend on the device. Ties are
 * not an edge case; in the largest script most of the cast shares a line count
 * with somebody.
 */
export function rankedCast(script) {
  const counts = new Map();
  for (const act of script?.acts || []) {
    for (const scene of act.scenes || []) {
      for (const line of scene.lines || []) {
        if (isStageDirections(line.actor)) continue;
        counts.set(line.actor, (counts.get(line.actor) || 0) + 1);
      }
    }
  }
  return [...counts.entries()]
    .sort((a, b) => b[1] - a[1] || (a[0] < b[0] ? -1 : a[0] > b[0] ? 1 : 0))
    .map(([actor]) => actor);
}

/**
 * Colours for a whole cast.
 *
 * `overrides` maps an actor name to an index into the picker ramp. Two roles
 * are allowed to end up with the same colour that way; it is a deliberate
 * choice by the user, not something to protect them from.
 *
 * `hueIndex` is the ramp entry in force, or null when the role still has the
 * colour its rank gives it, which is what the picker marks as selected.
 */
export function assignActorColors(ranked, overrides = {}) {
  const colors = {};
  ranked.forEach((actor, rank) => {
    const override = overrides[actor];
    const pinned = Number.isInteger(override);
    colors[actor] = {
      ...(pinned ? hueColor(override) : paletteColor(rank)),
      hueIndex: pinned ? ((override % HUE_CHOICES.length) + HUE_CHOICES.length) % HUE_CHOICES.length : null
    };
  });
  return colors;
}

const cache = new Map();

/**
 * Colours for a script, memoised.
 *
 * The largest script renders several thousand lines, so this must not run per
 * line. The key covers the cast order and the overrides, so both an edited
 * script and a newly picked colour recompute.
 */
export function actorColorsForScript(script, overrides = {}) {
  const ranked = rankedCast(script);
  if (ranked.length === 0) return {};
  const key = ranked.join('\u0000') + '\u0001' +
    Object.keys(overrides).sort().map(k => k + '=' + overrides[k]).join(',');
  let colors = cache.get(key);
  if (!colors) {
    colors = assignActorColors(ranked, overrides);
    cache.set(key, colors);
  }
  return colors;
}
